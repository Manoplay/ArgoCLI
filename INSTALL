# Installazione
Per installare le dipendenze richieste è sufficiente eseguire da riga di comando il seguente comando:
> python3 -m pip install -r requirements.txt

Assicurarsi che `python` e `pip` sia installato, questo può variare in base al sistema operativo in uso.